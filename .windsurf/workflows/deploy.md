---
description: Déployer le site Jekyll sur GitHub Pages
---

# Déployer le site sur GitHub Pages

1. Vérifier que toutes les modifications sont commitées
   ```bash
   git status
   ```

2. Ajouter les fichiers modifiés
   ```bash
   git add .
   ```

3. Commiter les changements
   ```bash
   git commit -m "Mise à jour du site"
   ```

4. Pousser vers GitHub
   ```bash
   git push origin main
   ```

5. Le site sera automatiquement déployé via GitHub Pages
