# Configurer le registre npm (par défaut: https://registry.npmjs.org/)
registry=https://registry.npmjs.org/

# Toujours ajouter des préfixes exacts lors de l'installation
save-exact=true

# Désactiver les packages scripts pour la sécurité
# (peut être réactivé si nécessaire)
# ignore-scripts=true

# Configurer les dépendances optionnelles pour éviter les installations inutiles
optional=false

# Forcer npm à générer un package-lock.json
package-lock=true

# Configurer le moteur de rendu npm pour les versions plus récentes
# (utile pour les problèmes de compatibilité sur Windows)
# node-options=--openssl-legacy-provider

# Configurer le cache npm (optionnel)
# cache=${APPDATA}/npm-cache
# cache-min=10
# cache-max=999999

# Configurer les logs (optionnel)
# loglevel=warn
# progress=false

# Configurer les dépendances de développement
# (peut être utile pour les environnements CI/CD)
# production=false
# audit=false
# fund=false

# Configurer les préfixes de script
# (utile pour les projets avec des scripts personnalisés)
# script-shell=bash

# Configurer les préférences de sécurité
# (recommandé pour la sécurité)
# engine-strict=true
# strict-ssl=true

# Configurer les préfixes de dépendance
# (utile pour les monorepos ou les workspaces)
# workspace=*
# workspaces-prefix=package

# Configurer les métadonnées du package
# (utile pour la publication)
# access=public
# tag=latest

# Configurer les préférences de téléchargement
# (peut améliorer les performances sur certains réseaux)
# prefer-offline=true
# prefer-online=false

# Configurer les préférences de mise en cache
# (utile pour le développement local)
# cache-max=0
# cache-min=10
# cache-lock-stale=0
# cache-lock-retries=10
# cache-lock-wait=10000

# Configurer les préférences de réseau
# (utile pour les connexions lentes ou limitées)
# fetch-retries=2
# fetch-retry-factor=10
# fetch-retry-mintimeout=10000
# fetch-retry-maxtimeout=60000

# Configurer les préférences de sortie
# (pour un meilleur formatage de la sortie)
# color=true
# depth=0
# long=false
# json=false

# Configurer les préférences de mise à jour
# (pour contrôler les mises à jour automatiques)
# update-notifier=false
# update-notifier-timeout=1000

# Configurer les préférences de sécurité supplémentaires
# (pour une sécurité renforcée)
# audit-level=low
# audit-level=moderate
# audit-level=high
# audit-level=critical

# Configurer les préférences de publication
# (pour la publication de packages)
# save-prefix=^
# save-dev-prefix=^
# save-optional-prefix=^
# save-exact=false
# save-prefix=~
# save-dev-prefix=~
# save-optional-prefix=~
