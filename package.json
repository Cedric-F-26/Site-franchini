{
  "name": "site-franchini",
  "version": "1.0.0",
  "description": "Site web pour la concession Franchini - Concessionnaire Deutz-Fahr",
  "private": true,
  "scripts": {
    "preinstall": "bundle config set --local path 'vendor/bundle' && bundle config set --local without 'development test'",
    "build": "bash build.sh",
    "start": "bundle exec jekyll serve --livereload --trace"
  },
  "engines": {
    "node": "18.x",
    "ruby": "3.3.0",
    "npm": ">= 8.19.0"
  },
  "dependencies": {
    "@vercel/static-build": "^1.3.0"
  }
}