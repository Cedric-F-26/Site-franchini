{
  "name": "site-franchini",
  "version": "1.0.0",
  "description": "Site web personnel",
  "private": true,
  "scripts": {
    "vercel-build": "bundle install && bundle exec jekyll build --trace"
  },
  "engines": {
    "node": "18.x",
    "ruby": "3.2.0"
  },
  "dependencies": {
    "@vercel/static-build": "^1.3.35"
  },
  "browserslist": ["defaults"]
}