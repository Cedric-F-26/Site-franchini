{
  "name": "site-franchini",
  "version": "1.0.0",
  "description": "Site web de Franchini - Concessionnaire Deutz-Fahr",
  "private": true,
  "scripts": {
    "build": "bundle exec jekyll build --config _config.yml,_config_vercel.yml --trace",
    "minify": "gulp",
    "clean": "gulp clean",
    "dev": "gulp watch"
  },
  "engines": {
    "node": ">= 16.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "del": "^7.0.0",
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  },
  "browserslist": [
    "defaults"
  ],
  "vercel": {
    "outputDirectory": "_site"
  }
}
