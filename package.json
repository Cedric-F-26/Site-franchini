{
  "name": "site-franchini",
  "version": "1.0.0",
  "description": "Site web de Franchini - Concessionnaire Deutz-Fahr",
  "private": true,
  "scripts": {
    "dev": "bundle exec jekyll serve --watch --port $PORT --host 0.0.0.0",
    "build": "bundle exec jekyll build --config _config.yml,_config_vercel.yml",
    "start": "bundle exec jekyll serve --port $PORT --host 0.0.0.0",
    "test": "echo \"Tests non configurés\" && exit 0",
    "vercel-build": "npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.8"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "prettier": "^3.4.0"
  },
  "browserslist": [
    "defaults"
  ]
}
